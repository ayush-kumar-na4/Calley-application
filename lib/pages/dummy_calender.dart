import 'package:flutter/material.dart';

class DummyCalender extends StatelessWidget {
  const DummyCalender({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "No Content Given By \n UI Designer For This Page",
        style: TextStyle(fontSize: 20),
        textAlign: TextAlign.center,
      ),
    );
  }
}
